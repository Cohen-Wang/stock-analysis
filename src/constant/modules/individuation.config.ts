// 主题配置
const theme = {
  config: [
    { label: '朴素白', value: 'light' },
    { label: '经典黑', value: 'dark' }
  ],
  default: 'light'
}

// 默认菜单宽度
const collapsed = {
  default: false
}

export default {
  theme,
  collapsed
}
